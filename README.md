# Api-built
Building the API using the Node and Express

# setup
1. Install the node 
2. clone the repository
3. go to `API-BUILT/backend` and run
  ```
  npm i
  ```
4. Now run
  ```
  npm run dev
  ```